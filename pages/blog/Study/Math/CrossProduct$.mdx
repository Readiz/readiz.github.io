---
title: 벡터의 외적
writtendate: 2023-05-25
tags:
    - cross product
    - math
---
import Comment from '@/components/Comment'
import Details from '@/components/Details'
import Image from '@/components/Image'
import Header from '@/components/Header'
import TagList from '@/components/TagList'

<Header />

벡터의 외적은 두 벡터가 이루는 각도를 알아낼 때 매우 유용하다. 특히, *두 벡터가 서로 평행일 때 외적값이 0*이 된다는 사실은 잘 알려져 있다.

## 기초를 튼튼히

평면에서의 외적값은 기본적으로 3차원으로 생각한다. 왜냐면, 물리학에서 배웠던 `무슨무슨 왼손법칙`처럼 기본적으로 외적값이라는 것은 방향 개념이 따라오는데, 이 방향이 2차원 축을 뚫고 나가기 때문이다. `i`, `j`, `k`가 각각 3차원의 기본 방향 성분이라고 할 때, 다음이 성립한다.

1. `i`*`j` = `k`
2. `j`*`k` = `i`
3. `k`*`i` = `j`

또한, 방향 개념이 있기 때문에, 만약 순서를 반대로 곱한다면 음수가 붙는 것처럼 된다. (방향이 반대라는 뜻)

1. `j`*`i` = -`k`
2. `k`*`j` = -`i`
3. `i`*`k` = -`j`

## 외적의 계산

위 식은 이제 unit vector에 대한 이야기이고, 실제로 임의의 두 벡터의 값은 어떻게 구할까? 일단 평면상에 두 점이 있다고 하면, `k` 값은 `0`으로 고정시킬 수 있을 것이다. 이 때의 외적값은 `a = (x1, y1)`, `b = (x2, y2)` 라고 하면, `a*b = (0,0,x1y2 - x2y1)` 이 된다. 이것은 다음을 의미한다.

1. 방향은 평면에서 오른손으로 `a -> b` 방향으로 감싸쥔 방향으로 적용된다. (z축이냐, -z축이냐의 차이만 있다)
2. 벡터의 크기는 `x1y2 - x2y1`이다.

그렇다면 일반적인 임의의 두 벡터의 외적은 어떻게 계산될까? 두 벡터를 (x1, y1, z1), (x2, y2, z2)로 정의하면, 결과값의 `x`, `y`, `z`를 나눠서 보면 아래처럼 된다.

1. `x` = `y1z2 - y2z1`
2. `y` = `z1x2 - x1z2`
3. `z` = `x1y2 - y1x2`

신기하게도 `x`를 구할 때에는 `x`가 없는 형태이다. 이를 `3*3` 행렬식으로는 아래처럼도 나타낸다. `x`자 모양으로 세번 곱해서 빼는 형태로도 기억할 수 있다.

|i |j |k |
|:-:|:-:|:-:|
|x1|y1|z1|
|x2|y2|z2|

## 외적의 크기

이는 쉽게 증명될 수 있지만 결과만 기억하자.

- `|a*b| = |a|*|b|*sinΘ`

`sin` 값은 `180도`, `360도`에서 0이 되고, 이는 벡터가 왜 평행일 때 외적값이 0이 되는지를 알려준다.

## 오목과 볼록

오목과 볼록은 정하기 나름이지만, 아무튼 위 sinΘ 성질을 생각하면, `180도` 기준으로 그 부호가 바뀌는 것은 명백하다. 오목, 볼록의 기준을 나누는 값이 `180도`가 되므로, 음수, 양수에 따라 오목, 볼록을 판정하면 된다. `Convex Hull` 과 같은 알고리즘에서 사용되는 성질이다. 사실 이거 때문에 간만에 외적을 다시 봤다.

<TagList />
<Comment />
